/* ==========================================================================
   contact.js — Contact page behaviour
   Owner: Edourd

   Loaded only by frontend/pages/contact.html.
   Keep everything scoped to this page. Shared logic belongs in main.js.
   ========================================================================== */

"use strict";

/**
 * Initialise the Contact page.
 */
function initContactPage() {
    // Contact behaviour to be developed by Edourd.
}

document.addEventListener("DOMContentLoaded", initContactPage);
